# ArmNet Docs

Independent VitePress documentation site generated from `../weelinking-docs/markdown`.

```bash
bun install
bun run docs:dev
bun run docs:build
```

Set `DOCS_BRAND_NAME` and `DOCS_API_BASE` to customize generated brand/API examples.
