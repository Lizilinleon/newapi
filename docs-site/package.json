{
  "name": "armnet-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:docs": "node scripts/prepare-docs.mjs",
    "docs:dev": "bun run prepare:docs && vitepress dev --host 0.0.0.0",
    "docs:build": "bun run prepare:docs && vitepress build",
    "docs:preview": "vitepress preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
